[package]
name = "evolu-std-platform"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "std Platform implementation for Evolu — SystemTime + getrandom"

[dependencies]
evolu-core = { workspace = true }
getrandom = { workspace = true }
