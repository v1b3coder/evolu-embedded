[package]
name = "evolu-file-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "File-based storage backend for Evolu — simple Vec-backed, for demo/testing on std systems"

[dependencies]
evolu-core = { workspace = true }
